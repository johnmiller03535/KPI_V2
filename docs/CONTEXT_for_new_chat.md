# КОНТЕКСТ ПРОЕКТА — KPI Портал ГКУ МО «РЦТ»
> Передать в начало нового чата

---

## СТАТУС НА 2026-05-03

### Что реализовано и работает
- ✅ Фазы 1–3: Redmine-синхронизация, периоды, полный цикл KPI
- ✅ Admin-панель: Dark Cyber дизайн, 6 табов
- ✅ Подчинённость: таб + импорт из People export (хранится в PostgreSQL)
- ✅ KPI-конструктор: БД (5 таблиц) + API + UI (все 6 типов форм)
- ✅ Импорт xlsx: 124 показателя, 189 критериев, 91 карточка, 261 привязка (счётчик 124 — правильно ✅)
- ✅ Конструктор показателей: создание и редактирование всех 6 типов протестировано

### Что сделано в последней сессии (2026-05-03)
- ✅ Sticky-шапки и двухколоночный layout во всех 3 табах (Подчинённость, KPI-карточки, KPI-показатели)
- ✅ Нормализация подразделений в сайдбарах (normalizeUnit + Map-дедупликация)
- ✅ Общие хелперы в `frontend/src/utils/admin.ts`
- ✅ **Конструктор показателей (шаг K-pre)**: новая форма `IndicatorFormModal` для всех 6 типов:
  - `binary_auto`, `binary_manual` — только общие поля
  - `multi_binary` — редактор подпоказателей (новый тип, миграция k4l5m6n7o8p9)
  - `threshold` — числитель/знаменатель + визуальный редактор правил
  - `multi_threshold` — 2+ суб-блоков с правилами
  - `quarterly_threshold` — табы Q1-Q4 + «Скопировать активный квартал во все»
- ✅ Баги исправлены: `indicator_group` не сохранялась (пропущено в CREATE endpoint), модалка перекрывала навбар, footer перекрывал поля

---

## СТЕК И ИНФРАСТРУКТУРА

```
Backend:  FastAPI + SQLAlchemy (async) + PostgreSQL + Alembic
Frontend: Next.js 14 + TypeScript + Dark Cyber дизайн
AI:       YandexGPT (primary) + GigaChat (fallback, сейчас 401)
Redmine:  https://kkp.rm.mosreg.ru (API key: в .env)
Docker:   postgres (kpi_portal), backend (:8000), frontend (:3000), nginx (:80)
```

### Ключевые переменные .env
```
REDMINE_URL=https://kkp.rm.mosreg.ru
REDMINE_API_KEY=5f498fb9d36bd82c84edef1851e921b84a302dfa
KPI_TRACKER_ID=279
```

---

## АРХИТЕКТУРА БД (основные таблицы)

```
users                  — сотрудники (из Redmine)
periods                — отчётные периоды
kpi_submissions        — отчёты сотрудников
kpi_values             — оценки по каждому показателю (JSONB в submissions)
kpi_indicators         — библиотека показателей (124 шт)
kpi_criteria           — критерии (+ sub_type, order — миграция k4l5m6n7o8p9)
kpi_role_cards         — карточки должностей (91 шт)
kpi_role_card_indicators — показатели в карточке
kpi_change_requests    — запросы на изменение
subordination          — матрица подчинённости (PostgreSQL, lazy-seed из JSON)
```

---

## КЛЮЧЕВЫЕ БИЗНЕС-ДОКУМЕНТЫ (в папке docs/)

```
BL_subordination.md      — подчинённость (v2.0, финал)
BL_ai_assessment.md      — AI-оценка (v1.2, финал)
BL_kpi_cards.md          — KPI-карточки (финал)
BL_kpi_constructor.md    — конструктор показателей (финал)
BL_numeric_kpi.md        — числовые показатели (финал) ← следующий шаг
BL_admin_structure.md    — управление структурой (финал)
TESTING_AND_ROADMAP.md   — роадмап (Фазы 1-3 закрыты)
```

---

## ФЛОУ AI-ОЦЕНКИ (финальный)

```
1. Сотрудник → «Загрузить саммари»
   → time_entries из Redmine (поле comments = основа)
   → для каждого binary_auto показателя отдельное саммари через YandexGPT

2. Сотрудник редактирует саммари (textarea под каждым показателем)

3. «Отправить на проверку»
   → AI оценивает каждый binary_auto по его саммари
   → статус submitted, форма read-only
   → уведомление руководителю в Telegram

4. Руководитель → /review
   → видит саммари + AI-оценку + уверенность
   → может переопределить AI (manager_override)
   → оценивает binary_manual (✅/❌)
   → утверждает

5. PDF генерируется и прикрепляется к задаче в Redmine
```

---

## ТИПЫ KPI ПОКАЗАТЕЛЕЙ (все 6 реализованы в конструкторе ✅)

| Тип | Кто оценивает | Как |
|---|---|---|
| binary_auto | AI (YandexGPT) | По саммари трудозатрат |
| binary_manual | Руководитель | Кнопки ✅/❌ |
| multi_binary | Руководитель | Каждый подпоказатель отдельно ✅/❌, нужны все |
| threshold | Сотрудник вводит факт | Числитель/знаменатель → % → пороги |
| multi_threshold | Сотрудник (несколько пар) | Все подпоказатели должны быть выполнены |
| quarterly_threshold | Сотрудник | Пороги меняются по кварталам |

---

## ОТКРЫТЫЕ ЗАДАЧИ (по приоритету)

### 🔴 Критичные (следующий шаг — шаг K)
1. **Числовые KPI в форме сотрудника** — ввод числитель/знаменатель для threshold/multi_threshold/quarterly_threshold
2. Telegram-уведомления — проверить что доходят

### 🟡 Средние
3. Поиск в KPI-показателях — проверить после последних фиксов
4. Draft-статус workflow: показатель draft → утверждение (начальник → admin)
5. Тестовые показатели (созданные при тестировании) — удалить или перевести в archived
6. Тестирование на реальных сотрудниках (создать период для всех)
7. Роль HR — добавить в систему
8. Обновить GigaChat ключ (fallback)

### 🟢 Низкие
9. Activity types → KPI маппинг (шаг I)
10. Уведомления при изменении карточки KPI
11. Прогресс-бар в черновике 100% (старый баг)

---

## МАТРИЦА ПОДЧИНЁННОСТИ

```
РУК_ПЕРЗ_001 → ПРА_НАЧ_032, КЗА_НАЧ_042
РУК_ЗАМД_002 → ЗПД_НАЧ_052, ЗПР_НАЧ_061
РУК_ЗАМД_003 → ЕАС_НАЧ_019
РУК_ЗАМД_004 → ЦТР_НАЧ_071, ААД_НАЧ_081
ОРГ_НАЧ_005  → подчиняется директору (вне бота)
```

Синхронизация: вручную через People export из Redmine → кнопка «Импорт из People» в admin.
Хранение: PostgreSQL таблица `subordination` (lazy-seed из `reference/subordination.json`).

---

## ИЗВЕСТНЫЕ БАГИ

| Баг | Статус |
|---|---|
| Прогресс-бар в черновике 100% | Открыт |
| GigaChat 401 (fallback недоступен) | Открыт — нужно обновить ключ |

---

## ВАЖНЫЕ ТЕХНИЧЕСКИЕ ДЕТАЛИ

- `employees.position_id` хранит числовой `pos_id` (`"71"`), не `role_id` (`"ЦТР_НАЧ_071"`)
- `has_kpi_card` в subordination API — сравнение через `int()` cast
- `indicator_group` — обязательно передавать в `KpiIndicator()` при CREATE (был баг, исправлен)
- `kpi_criteria.sub_indicators` JSONB — используется для multi_binary (`sub_type: 'sub_binary'`) и multi_threshold
- Миграции Alembic: последняя — `k4l5m6n7o8p9` (sub_type + order в kpi_criteria)

---

## КАК НАЧАТЬ НОВЫЙ ЧАТ

Скинь этот файл в начало нового чата и напиши:
«Продолжаем работу над KPI-порталом. Прочитай контекст и подтверди что всё понял.
Текущая задача: шаг K — числовые KPI в форме сотрудника (threshold/multi_threshold/quarterly_threshold).
Начни с чтения `docs/BL_numeric_kpi.md`»
