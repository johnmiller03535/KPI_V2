# ЗАДАЧА: Снять ограничение на бэкенде (не только фронтенде)

> Файл для Claude Code | Дата: 2026-05-04 | Приоритет: 🔴

## Проблема

Предыдущий фикс снял ограничение только на фронтенде.
Бэкенд всё ещё возвращает 400: «Структурные поля можно менять только у черновика».

## Что сделать

В `backend/app/api/admin.py` найти эндпоинт `PATCH /indicators/{id}` (или аналогичный update).
Найти проверку вида:

```python
if indicator.status == 'active':
    raise HTTPException(400, "Структурные поля можно менять только у черновика")
```

**Закомментировать** с пометкой:
```python
# TODO: АУДИТ 2026-05-04 — временно отключено для полного редактирования
# if indicator.status == 'active':
#     raise HTTPException(400, "Структурные поля можно менять только у черновика")
```

Также проверить `backend/app/services/kpi_constructor.py` или аналогичный сервис —
проверка может быть там.

## Файл
```
backend/app/api/admin.py
backend/app/services/kpi_constructor.py  (если есть)
```
