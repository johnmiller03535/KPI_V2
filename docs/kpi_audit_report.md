# Аудит KPI-показателей — 2026-05-04

## Общая статистика

| Параметр | Значение |
|---|---|
| Всего показателей в БД | 132 |
| Активных (`active`) | **124** ✅ |
| Черновиков (`draft`) | 8 (тестовые, созданы ZaichkoVV) |
| Общих `is_common=true` | **3** ✅ |
| Карточек должностей | **91** ✅ |
| Привязок показателей к карточкам | **443** (≈4.87 на карточку, мин 4, макс 7) |

### Активные показатели по типам

| Тип | Кол-во |
|---|---|
| `binary_auto` | 69 |
| `threshold` | 44 |
| `multi_threshold` | 6 |
| `binary_manual` | 3 |
| `quarterly_threshold` | 2 |
| **Итого** | **124** |

---

## ✅ Без проблем

- Все 124 активных показателя привязаны хотя бы к одной карточке
- Нет дублирующихся привязок (один показатель дважды в одной карточке)
- Нет карточек без показателей
- Нет карточек с суммой весов ≠ 100%
- Все показатели имеют критерий

---

## ⚠️ Проблемы требующие внимания

### 1. Тестовые показатели в статусе `draft` (8 шт)

Созданы при тестировании конструктора. Нужно удалить или перевести в `archived`.

| Название | Тип |
|---|---|
| Тестовый авто-показатель | binary_auto |
| Тестовый показатель | binary_manual |
| Тестовый ручной показатель | binary_manual |
| Тестовый составной показатель | multi_binary |
| Тестовый мульти-пороговый показатель | multi_threshold |
| Тестовый квартальный показатель | quarterly_threshold |
| Тестовый пороговый показатель | threshold |
| Тестовый пороговый показатель 2 | threshold |

### 2. Пустой `denominator_label` у 9 активных threshold-показателей

Числитель заполнен (как описание логики), знаменатель пустой.
Для шага K (ввод факта сотрудником) нужны оба поля — это потребует ручного заполнения через конструктор.

| Название | Тип |
|---|---|
| Обеспечение моделирования бизнес-процессов | multi_threshold |
| Подготовка аналитических материалов по ЗИТ | multi_threshold |
| Подготовка информационных материалов по популяризации ЗИТ | multi_threshold |
| Количество участников на торгах по МО | quarterly_threshold |
| Обеспечение заказчиками МО снижения рисков срыва закупок (223-ФЗ) | threshold |
| Обеспечение контроля направления отчётности в ИФНС, Росстат | threshold |
| Оптимизация выпуска документов (локальные акты) | threshold |
| Оптимизация локальных актов Учреждения | threshold |
| Правовая экспертиза проектов локальных актов | threshold |

### 3. Пустые пороги (`thresholds = []`) у 3 active + 2 draft показателей

Active без порогов:
| Название | Тип |
|---|---|
| Оптимизация локальных актов Учреждения | threshold |
| Своевременная подготовка заключений по НПА | threshold |
| Соблюдение сроков подготовки ответов | threshold |

Draft без порогов (тестовые, не критично):
- Тестовый мульти-пороговый показатель
- Тестовый квартальный показатель

### 4. Все multi_threshold имеют по 1 критерию

Ожидается 2+ подпоказателей — нужно проверить логику импорта xlsx.
В карточках используются корректно (привязки есть), но поведение в форме сотрудника может быть неожиданным.

---

## Рекомендации

1. **Удалить тестовые показатели** — 8 draft-показателей (ZaichkoVV) — через UI или SQL
2. **Заполнить denominator_label** для 9 показателей — через конструктор (шаг K требует это)
3. **Заполнить пороги** для 3 активных threshold-показателей без правил
4. **Проверить multi_threshold** — должны ли они иметь несколько sub-критериев?

---

## Итог

Основные данные корректны: **124 active показателя, 91 карточка, 0 дублей, 0 нарушений весов**.
Проблемы некритичные, но требуют ручного заполнения перед запуском шага K.
