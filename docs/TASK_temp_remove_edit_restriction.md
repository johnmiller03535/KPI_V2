# ЗАДАЧА: Временно снять ограничение на редактирование активных показателей

> Файл для Claude Code | Дата: 2026-05-04 | Приоритет: 🔴
> ⚠️ ВРЕМЕННОЕ ИЗМЕНЕНИЕ — вернуть ограничение после завершения аудита

---

## Что сделать

Найти в backend проверку которая блокирует редактирование структурных полей
у показателей со статусом `active` и **временно отключить её**.

Скорее всего в `backend/app/api/admin.py` в эндпоинте `PATCH /indicators/{id}`:

```python
# БЫЛО — блокировка структурных полей для active:
if indicator.status == 'active' and has_structural_changes(body):
    raise HTTPException(400, "Структурные поля можно менять только у черновика")

# СТАЛО — временно убрать проверку (закомментировать):
# if indicator.status == 'active' and has_structural_changes(body):
#     raise HTTPException(400, "Структурные поля можно менять только у черновика")
```

Также убрать соответствующую проверку на фронтенде если она есть
(в `IndicatorFormModal` — возможно поля делаются `disabled` для active показателей).

## Важно

Добавить TODO-комментарий рядом с закомментированной проверкой:
```python
# TODO: АУДИТ 2026-05-04 — проверка временно отключена для полного редактирования
# Вернуть после завершения аудита показателей
```

## Файлы

```
backend/app/api/admin.py          ← убрать HTTPException
frontend/src/app/admin/page.tsx   ← убрать disabled если есть
```
