# Задача: Исправить содержимое PDF отчёта
> Файл: backend/app/services/report_service.py

---

## ПРАВКА 1 — Колонка «Мероприятия по выполнению показателя»

### Было
Для binary_auto показателей в колонку попадает обоснование AI:
«В саммари не указано, что были проведены работы...»

### Стало
В колонку «Мероприятия» всегда писать `summary_text` из submission.
Это текст который сотрудник подготовил и отредактировал.

```python
# Для ВСЕХ показателей в колонке «Мероприятия»:
activities_text = submission.summary_text or '—'
```

Обоснование AI (`ai_reasoning`) — убрать из этой колонки полностью.
Пометку «[Решение руководителя: Выполнено]» оставить, но перенести
в колонку «Выполнение целевого значения» или добавить сноской.

---

## ПРАВКА 2 — Стандартные формулировки для общих показателей

Для показателей с `is_common=True` и `formula_type=binary_manual`
вместо прочерка «—» в колонке «Мероприятия» подставлять стандартный текст.

### Словарь стандартных формулировок

```python
COMMON_KPI_TEXTS = {
    # Ключ — подстрока из criterion (регистронезависимо)
    "исполнительской дисциплины": {
        True:  "Исполнительская дисциплина соблюдается в полном объёме. Сроки исполнения протокольных поручений, образующихся в ходе деятельности Учреждения, а также сроки исполнения приказов и распоряжений Учреждения, письменных и устных поручений руководства не нарушаются.",
        False: "Исполнительская дисциплина не соблюдается в полном объёме. Допущены нарушения сроков исполнения протокольных поручений, приказов и распоряжений Учреждения."
    },
    "трудового распорядка": {
        True:  "Правила внутреннего трудового распорядка и Кодекса этики соблюдаются в полном объёме.",
        False: "Правила внутреннего трудового распорядка и Кодекса этики не соблюдаются в полном объёме."
    },
    "техники безопасности": {
        True:  "Правила и нормы техники безопасности, охраны труда и противопожарного режима соблюдаются в полном объёме.",
        False: "Правила и нормы техники безопасности, охраны труда и противопожарного режима не соблюдаются в полном объёме."
    },
}

def get_common_kpi_text(criterion: str, is_fulfilled: bool) -> str:
    criterion_lower = criterion.lower()
    for key, texts in COMMON_KPI_TEXTS.items():
        if key in criterion_lower:
            return texts[is_fulfilled]
    # Fallback для неизвестных общих показателей
    if is_fulfilled:
        return "Показатель выполнен в полном объёме."
    else:
        return "Показатель не выполнен."
```

### Применение в PDF

```python
for kpi in kpi_values:
    if kpi.is_common and kpi.formula_type == 'binary_manual':
        is_fulfilled = kpi.manual_score  # True/False
        activities = get_common_kpi_text(kpi.criterion, is_fulfilled)
    elif kpi.formula_type == 'binary_auto':
        activities = submission.summary_text or '—'
    elif kpi.formula_type == 'binary_manual':
        activities = submission.summary_text or '—'
    else:
        # numeric/threshold
        activities = f"Факт: {kpi.fact_value}" if kpi.fact_value else '—'
```

---

## Чеклист
- [ ] Колонка «Мероприятия» для binary_auto → summary_text
- [ ] Колонка «Мероприятия» для is_common binary_manual → стандартный текст
- [ ] Стандартный текст зависит от результата (выполнено/не выполнено)
- [ ] AI reasoning убран из колонки «Мероприятия»
- [ ] Пересобрать и проверить PDF
